namespace ExclusiveCard.IntegrationTests.Public
{
    public class _528FeatureImage
    {
        //addMerchantFeatureImage
        // url: "/Merchant/SaveFeatureImage",
        //type: "POST",

        //var formData = new FormData();
        //formData.append('id', id);
        //formData.append('imageFile', file);

        //[Test]
        //public async Task SaveFeatureImage()
        //{
            //MapImageToBlobStorage is built into the MerchantControler !!!! aaagggh
            //int id = 0;
            //    string fileName = Guid.NewGuid().ToString("N").ToUpper();
            //    Bitmap image = Image.FromFile(@"D:\SourceCode\SVGs\RedBallOrangeBall.svg") as Bitmap;

            //    List<MerchantImageViewModel> sessionImages = new List<MerchantImageViewModel>();
            //    sessionImages.Add(new MerchantImageViewModel() { ImageType = (int)ImageType.FeatureImage });

            //    List<MerchantImageViewModel> merchantImageViews = await MapImageToBlobStorage(id, image, fileName, sessionImages, ImageType.FeatureImage);

            //    var result = merchantImageViews.FirstOrDefault(x => x.ImageType == (int)ImageType.FeatureImage);
        //}
    }
}
